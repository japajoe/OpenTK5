# OpenTK5
A framework for rendering 3D graphics with OpenGL. This library combines all of OpenTK into a single binary. I do not actively maintain this repository so don't expect any bugfixes here.

# Installation
```
dotnet add package JAJ.Packages.OpenTK5 --version 2.0.0
```
